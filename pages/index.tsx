import type { NextPage } from "next";
import Head from "next/head";
import Image from "next/image";
import { WordSwitcher } from "../modules/layout/WordSwitcher";
import { Button, UnstyledButton, useMantineTheme, useMantineColorScheme } from "@mantine/core";
import styles from "../styles/Home.module.css";
import { NextLink } from "@mantine/next";

const Home: NextPage = () => {

  const {colorScheme} = useMantineColorScheme();
  const theme = useMantineTheme();


  return (
    <div>
      <Head>
        <title>Create Next App</title>
        <meta name="description" content="Generated by create next app" />
        <link rel="icon" href="/favicon.ico" />
      </Head>
      <div className={styles.splashContainer}>
        <div style={{placeSelf: "center"}}>
          <h1>Welcome to Remy&apos;s Chat App.</h1>
          <h2>For {<WordSwitcher />}</h2>
          {process.env.NODE_ENV === "development" && <h2>Dev Mode</h2>}
          <p>Click on Rooms and select a room to get started!</p>
          <p>Don&apos;t try anything funny I will ban the shit out of you.</p>

          <NextLink href="/register">
            <Button>Register Today!</Button>
          </NextLink>
          <NextLink href="/login">
            <UnstyledButton mt={10} style={{color: theme.colors.whiteAlpha[8]}}>If you already have an account, click here to log in.</UnstyledButton>
          </NextLink>
        </div>
      </div>
    </div>
  );
};

export default Home;
